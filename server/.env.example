# 服务端口
PORT=8787

# 数据库（zero-mysql-dev 容器，映射在本机 3307）
DATABASE_URL=mysql://zero:zero@127.0.0.1:3307/zero

# JWT 签名密钥（生产环境务必替换为随机长字符串）
JWT_SECRET=change-me-in-prod

# 允许的前端来源（CORS）
CORS_ORIGIN=http://localhost:5173
